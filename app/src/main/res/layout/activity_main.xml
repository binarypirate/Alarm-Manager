<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/setAlarmBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="56dp"
        android:text="set alarm"
        android:onClick="onClickSetAlarm"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/miniutes" />

    <EditText
        android:id="@+id/hour"
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="152dp"
        android:hint="hours"
        android:inputType="numberDecimal"
        app:layout_constraintEnd_toStartOf="@+id/miniutes"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/miniutes"
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:hint="minutes"
        android:inputType="numberDecimal"
        app:layout_constraintEnd_toStartOf="@+id/seconds"
        app:layout_constraintStart_toEndOf="@+id/hour"
        app:layout_constraintTop_toTopOf="@+id/hour" />

    <EditText
        android:id="@+id/seconds"
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:hint="seconds"
        android:inputType="numberDecimal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/miniutes"
        app:layout_constraintTop_toTopOf="@+id/miniutes" />


</androidx.constraintlayout.widget.ConstraintLayout>